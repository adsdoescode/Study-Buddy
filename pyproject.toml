[project]
name = "study-buddy"
version = "0.1.0"
requires-python = ">=3.12"
dependencies = [
	"flask",
	"flask-cors",
	"python-dotenv",
	"google-generativeai",
]

[tool.vercel]
entrypoint = "app:app"
